import React from 'react';
import styles from './Cart.css'

const Cart = ({ selection }) => {
    console.log(selection);
    return (
        <div>
            <h3>My Selections </h3>


        </div>
    );
};

export default Cart;