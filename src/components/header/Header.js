import React from 'react';
import styles from './Header.css'

const Header = () => {
    return (
        <div>
            <h2 className='title'>
                Choice Maker
            </h2>
        </div>
    );
};

export default Header;